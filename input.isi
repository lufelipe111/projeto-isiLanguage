programa
    numero a,b,c,d,e,f.
    texto g.
    escreva("Programa Teste").
    escreva("Digite A").
    leia(a).
    escreva("Digite B").
    leia(b).

    g := 0.
    a := "tela".

    se(a<b) entao
    {
        c := a + b.
    }senao
    {
        c := a - b.
    }

    escreva ("C e igual a ").
    escreva(a).

    d := c * a + b.

    escreva ("D e igual a ").
    escreva(d).

fimprog.