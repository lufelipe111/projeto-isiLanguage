programa
    numero a,b,c,d,e,f.
    texto g.
    escreva("Programa Teste").
    escreva("Digite A").
    leia(a).
    escreva("Digite B").
    leia(b).

    g := "A eh menor que 2".

    se(a<2) entao
    {
        c := a + b.
    }senao
    {
        c := a - b.
    }

    enquanto (a>2) {
        escreva(g).
        a := a-1.
    }
    escreva ("C e igual a ").
    escreva(a).

    d := c * a + b.

    escreva ("D e igual a ").
    escreva(d).

fimprog.